#include <bits/stdc++.h>
using namespace std;

#define ll long long

struct pt
{
	int x, y;
};

void solve()
{
	int n;
	cin >> n;
	vector<pt> a(n);
	for (int i = 0; i < n; i++)
		cin >> a[i].x >> a[i].y;
	
}

int main()
{
	cin.tie(0) -> sync_with_stdio(0);
	int t;
	cin >> t;
	while (t--)
		solve();
	return 0;
}